/*
scrapped attempt at making another window show up

chrome.action.onClicked.addListener(function(tab) {
    chrome.windows.create({
      url: chrome.runtime.getURL("Bully-Busters/warning.html"),
      type: "popup"
    });
});
*/
